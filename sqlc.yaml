version: "2"
sql:
  - engine: "sqlite"
    schema: "sql/migrations/"
    queries: "sql/queries/"
    gen:
      go:
        out: "internal/api/dbmodels"
        json_tags_id_uppercase: true
        emit_json_tags: true
