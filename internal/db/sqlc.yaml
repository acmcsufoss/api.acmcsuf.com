version: "2"
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: "queries.sql"
    gen:
      go:
        package: "models"
        out: "models"
        json_tags_id_uppercase: true
        emit_json_tags: true
