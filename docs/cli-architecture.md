# CLI Architecture

CLI usage is designed to be similar to sending an HTTP request:

- URL query params are set via command line flags
- JSON body data is filled out with an interactive form or by submitting a raw JSON payload
> JSON payload is not yet implemented

